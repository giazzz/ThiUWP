<Page
    x:Class="NoteApp.Pages.Note"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoteApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entity="using:NoteApp.Entity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="0.5*"/>
            </Grid.ColumnDefinitions>
            <StackPanel x:ConnectionId='2' Grid.Column="0" x:Name="menu">
                <ListView x:ConnectionId='6' x:Name="listNote">
                    <ListView.ItemTemplate>
                        <DataTemplate                             >
                            <StackPanel x:ConnectionId='8' Margin="0,10,0,10" Orientation="Horizontal"                            Tag="{Binding}">
                                <TextBlock x:ConnectionId='9'                          Style="{ThemeResource BaseTextBlockStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
            <StackPanel x:ConnectionId='3' Grid.Column="1" x:Name="note">
                <TextBlock>Note</TextBlock>
                <TextBox x:ConnectionId='4' x:Name="noteEdit"/>
                <Button x:ConnectionId='5'                     >Save</Button>
            </StackPanel>
        
        </Grid>
    </ScrollViewer>
</Page>

